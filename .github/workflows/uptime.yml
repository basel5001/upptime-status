name: Uptime CI
on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:
jobs:
  uptime:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: upptime/uptime-monitor@v1.6.2
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
